# Core API
fastapi==0.110.0
uvicorn==0.29.0
pydantic==2.6.4
httpx==0.27.0

# LLM / Agents
openai==1.30.0
langgraph==0.0.38
langchain==0.1.13

# HuggingFace ecosystem
transformers==4.40.0
peft==0.10.0
accelerate==0.27.2
datasets==2.18.0
sentence-transformers==2.6.1
bitsandbytes==0.43.0

# Vector store
faiss-cpu==1.8.0

# Distributed / async
celery==5.3.6
redis==5.0.3
ray[train]==2.10.0

# ML / data science
torch==2.2.1
torchvision==0.17.1
numpy==1.26.4
scipy==1.12.0
scikit-learn==1.4.1
pandas==2.2.1
matplotlib==3.8.3
umap-learn==0.5.6

# MLOps
mlflow==2.11.1
ragas==0.1.7

# RLHF / RL fine-tuning
trl==0.8.6

# AWS
boto3==1.34.0
sagemaker==2.214.0

# Azure
azure-identity==1.16.0
azure-mgmt-appcontainers==3.0.0
azure-ai-ml==1.15.0
azure-monitor-ingestion==1.0.0

# Data ingestion
pyspark==3.5.1
delta-spark==2.4.0
warcio==1.7.4
langdetect==1.0.9
datasketch==1.6.4

# Diffusion / multimodal
diffusers==0.27.2
pillow==10.2.0
opencv-python==4.9.0.80
timm==0.9.16

# Microservices / event-driven
aiokafka==0.10.0               # Kafka async producer/consumer (microservices event bus)
aioredis==2.0.1                # Redis Streams for event bus fallback
kafka-python==2.0.2            # Sync Kafka producer/consumer (streaming pipeline)

# MCP server
mcp>=1.0.0

# Local inference (llama.cpp)
llama-cpp-python>=0.2.57       # CPU by default; rebuild with CMAKE_ARGS for CUDA/Metal
huggingface-hub>=0.22.0        # GGUF model downloads

# TensorRT / ONNX / Triton
onnx>=1.16.0
onnxruntime>=1.17.0
# tritonclient[http]>=2.44.0   # uncomment when Triton server is available
# tensorrt>=10.0.0              # requires NVIDIA GPU + CUDA; install from NVIDIA wheel

# Quantization
auto-gptq>=0.7.1               # GPTQ 4-bit quantisation
autoawq>=0.2.4                 # AWQ 4-bit quantisation

# Configuration management
hydra-core>=1.3.2
omegaconf>=2.3.0

# Evaluation UI
gradio>=4.26.0

# Testing / linting
pytest==8.1.1
pytest-asyncio==0.23.6
pytest-cov==5.0.0
ruff==0.3.4
black==24.3.0
# Observability
prometheus-client>=0.20.0          # Prometheus metrics for FastAPI gateway

# Multi-GPU / MoE serving
# deepspeed>=0.14.0                 # DeepSpeed-Inference for MoE serving; install separately
# ray[serve]>=2.10.0                # Ray Serve multi-replica deployment (already in ray[train])
